# Include J-Link interface support
source [find interface/jlink.cfg]

# Select SWD transport
transport select swd

# Include target MCU
source [find target/stm32h7x.cfg]

# Optional: Configure RTT if needed
# rtt setup 0x20000000 1024 "SEGGER RTT"  # Adjust address and size as per your application
# rttserver start 9090 0